# План из трёх частей

Личный дедлайн готовности и сдачи: **17:50, Asia/Qyzylorda (UTC+5)**. По скриншоту сообщения организатора, переданному участником, присутствовать нужно **до 18:00 даже при готовом решении**. Это не перенос личного дедлайна. Точное время в ранее предоставленных PDF не указано.

**Текущий production:** `a3313b8196d4f5a47e775077e52dbf4d68113bd9`; push **16:32:40**, локальный SHA совпадает с `origin/main`. Vercel **Ready**, deployment `dpl_AGZwHSGrQsWirGubs3m5cYWPoY7L`. [Основной адрес](https://dir-echoes-voice-router.vercel.app) · [адрес этого деплоя](https://dir-echoes-voice-router-4slrsmsra-shadowocc.vercel.app). Локальные `pnpm build` / TypeScript и Vercel build прошли; health HTTP 200, `ok`, подтверждён в **16:33**. Production после перезагрузки показал новый UI; favicon указывает на `/brand/dir-echoes-mark.png`. При фактическом viewport **600×944** горизонтального переполнения нет. Прежняя собственная история проверена локально; её повторная проверка на этом production reload не заявляется. Живой мобильный голос и системная клавиатура не приняты.

В `a3313b8` опубликованы выбранный знак №4 «птица» в меню/входе/favicon и исправления частиц: до **3400** точек, **1800** на мобильных/ограниченных устройствах, пакетная отрисовка без покадровой сортировки. **60 FPS — цель планировщика, не измеренный FPS.** Реакция на курсор и сохранение короткого измеренного RMS исправлены в коде; 180 фоновых звёзд независимы. Это не подтверждает устранение сообщённого пользователем голосового сбоя: живая приёмка остаётся открытой.

## Часть 1. Требования, архитектура и постоянная основа

**Результат:** основа реализована; исходные материалы остаются вне Git.

- [x] Прочитаны четыре PDF и состав официального набора; пользователь подтвердил работу с данными организатора.
- [x] Выбран единственный командный репозиторий `BAITC-Hacks/hack-6d253846-dir-echoes`; документация отправлена коммитом `00b1979`, основной код — `ac18111`.
- [x] Реализован Next.js / TypeScript проект, серверные контракты, вход по двум ролям и проверка доступа.
- [x] Реализованы схема PostgreSQL, транзакции, версия состояния разговора и уникальный request ID.
- [x] Реализован импорт с проверкой 40 сценариев, 43 слотов, 31 действия, ссылок и SHA-256.
- [x] Повторный импорт той же версии сохраняет изменения записей; другая версия требует явной миграции.
- [x] Подключена Neon; локальный вход участника и первый сохранённый разговор подтверждены. Для разработки также предусмотрена PGlite.
- [x] Реальные обращения к OpenAI подтверждены; выполнен исторический baseline 104 текстовых реплик до последующих изменений ядра и языков.
- [x] Зафиксировать использование облачной базы и сохранность данных именно опубликованной версией: две реплики и trace сохранены после перезагрузки, 14:48; [подробности](CHECKPOINTS.md).

История отправленных контрольных точек — в [CHECKPOINTS.md](CHECKPOINTS.md). Наличие файлов локально не означает их присутствие на GitHub или Vercel.

## Часть 2. Разговор, действия и интерфейс

**Результат:** основной код реализован; проверки отдельных границ пройдены, живое прослушивание ещё требуется.

- [x] LLM-router по 40 сценариям и 3 системным намерениям, Structured Outputs, альтернативы и краткое обоснование.
- [x] В коде RU / KK / TR / mixed и массивы конкретных входных языков и языков ответа; смешанные RU/KK, RU/TR, KK/TR и три языка не сводятся к общей метке без состава.
- [x] Детерминированные TR-вопросы, ошибки и подтверждения действий; качество TR и новых смешанных пар ещё не измерено.
- [x] Сбор параметров, поиск клиента, проверка владения, уточнение и переход к оператору.
- [x] Очередь намерений, срочный приоритет, сохранение прерванной темы и возврат.
- [x] 31 обработчик действий и расчёты из официальной базы знаний.
- [x] Все изменения бизнес-данных и запросы внешних действий: сохранённые параметры → явное подтверждение → атомарное изменение.
- [x] Повтор подтверждения не дублирует действие; изменение расчётных условий запрашивает новое согласие.
- [x] Сохранение истории, экспорт JSON, трассировка и показатели по фактическим записям.
- [x] Очередь супервизора: принять, ответить, закрыть; последующие сообщения клиента сохраняются в обращении.
- [x] Реализован цикл с одним стартом: локальная пауза 950 мс → отправка отдельной реплики в STT → ответ и потоковый TTS → возврат к прослушиванию. Микрофон приостановлен во время обработки и озвучивания; доступна ручная кнопка «Перебить ответ».
- [x] Ограничены загрузка и длина аудио, есть кэш TTS; остановка, уход с экрана и скрытие страницы завершают захват микрофона. Это half-duplex с локальным определением паузы, без потокового Realtime STT и автоматического прерывания голосом.
- [x] Проверена связность речевых API: TTS вернул MP3 61 056 байт, STT распознал этот сгенерированный ответ с HTTP 200 за 2084 мс. Это не проверка живого микрофона или качества code-switching.
- [x] Общий журнал оценочных расходов API, резервирование до запроса, ограничения частоты и длины.
- [x] Историческая версия исполнителя до расширения политики согласия прошла 19 целевых тестов. Этот результат не подтверждает текущее дерево после поздних изменений.
- [x] В `a3313b8` опубликованы голосовой экран, светлая/тёмная темы, скрываемое меню, прозрачный чат поверх сцены, подсказки и раскрываемые детали решения. Новый UI показан после production reload; собственная прежняя история подтверждена локально.
- [x] Историческая браузерная проверка `5aac0b5` при **390×844, 390×500 и 844×390** не выявила горизонтального переполнения. Она не переназначается новому деплою и не проверяет физическую клавиатуру ОС или живое аудио.
- [x] Исправления отрисовки частиц, реакции на курсор/RMS и выбранный знак опубликованы в `a3313b8`; реальный FPS и голосовая приёмка не подтверждены.
- [ ] Уточнить сообщение пользователя о проблеме голоса, воспроизвести конкретное отклонение и исправить подтверждённую причину.
- [ ] Проверить непрерывный цикл из нескольких живых реплик и слышимый ответ на RU, KK и их смешении; TR/RU-TR/KK-TR принять отдельно.
- [ ] Проверить физический мобильный браузер, разрешение микрофона и системную клавиатуру, включая возврат управления после её закрытия.
- [ ] Завершить проверку всех экранных действий в браузере и поведение при отказах голосовых API.

**Граница внешних действий:** outbox хранит запрос доставки; запись к врачу / на осмотр хранит запрос без подтверждённого времени; новые полисы ожидают оплаты. Очередь супервизора работает внутри приложения. Реальные SMS, внешняя телефония, эквайринг и расписания не подключены и не изображаются завершёнными.

Контрольные push: основной текстовый путь и исполнитель; интерфейс и голос; проверенные исправления. Значимый завершённый шаг следует фиксировать сразу.

## Часть 3. Приёмка, Vercel и сдача

**Результат:** историческая базовая оценка выполнена; опубликована и подтверждена `a3313b8` с новым интерфейсом. Человеческая приёмка голоса, актуального языкового качества и системной клавиатуры открыта. Доказательства и ограничения — в [контрольных точках](CHECKPOINTS.md).

- [x] Исторический baseline **до последующих изменений ядра**: все 104 официальные текстовые dev-реплики без API-ошибок; основной сценарий **99/104 (95,19%)**, полный набор **97/104 (93,27%)**, RU/KK mixed **7/7**.
- [x] В этом baseline медиана router **2308 мс**, оценка стоимости **около 0,294 USD**. Цель 500 мс не достигнута; замер не включает STT/TTS и сквозную задержку.
- [ ] Полный повторный прогон актуального ядра после последних изменений отсутствует. Новые TR-пары и живой голос старым baseline не подтверждаются.
- [x] README содержит проблему, технологии, настройку и запуск, а также шестишаговую проверку для жюри — согласно уточнению организатора на скриншоте.
- [x] Подтверждённая `a3313b8` доступна на [Vercel](https://dir-echoes-voice-router.vercel.app): Ready, health HTTP 200; новый интерфейс и favicon показаны на production; прежняя собственная история проверена локально. Ранние проверки 40 сценариев и супервизорского API остаются историческими доказательствами в CHECKPOINTS.
- [x] Реализованы супервизорские оценки и версии текстовых полей каталога; браузерная приёмка последней версии остаётся открытой.
- [x] Измерено узкое быстрое продолжение: 1200 локальных повторений без API, median 0,005 мс; три парных вызова LLM суммарно около 0,00638 USD. Это не точность намерений и не задержка голоса.
- [x] Для опубликованного `a3313b8` подтверждены TypeScript и production-build. После следующего изменения результаты нужно привязать к новому SHA.
- [ ] Зафиксировать результат актуальных unit/API-проверок для коммита сдачи; прежние результаты не объявляются текущими.
- [x] На локальном `a3313b8` в браузере доступна ранее сохранённая собственная история.
- [ ] Проверить новую запись с перезагрузкой, очередь и разделение доступа между участниками на итоговой версии.
- [ ] Проверить фактические файлы будущего коммита: без секретов, исходного набора, PDF, локальных отчётов и баз.
- [x] Опубликовать текущий checkpoint с постоянной PostgreSQL и записать SHA/deployment ID: `a3313b8196d4f5a47e775077e52dbf4d68113bd9`, `dpl_AGZwHSGrQsWirGubs3m5cYWPoY7L`. Финальную сдаваемую версию обновить после оставшихся исправлений.
- [x] Проверить статус `Ready`, health и доступность нового интерфейса на текущем опубликованном checkpoint.
- [ ] Принять текст, живой микрофон, слышимый ответ, новые подтверждения и супервизию на последней опубликованной версии.
- [x] Историческая проверка `cced122`: `onplaying` через 2,72 с при одном текстовом продолжении, без console warning/error. Это не измерение нового цикла, живой речи или слышимости; цель 1,5 с не подтверждена.
- [ ] Выполнить финальный commit/push, проверить совпадение локального и удалённого SHA.
- [ ] Отдельно открыть платформу мероприятия и сохранить **«Сдать решение»** для нужного кейса.

Резерв команды: после **17:20** сосредоточиться на исправлениях и сдаче; до **17:35** завершить финальный push и оставить время для формы и проверки. Это рабочий план команды, а не правило организаторов.

Дополнительное требование участника: сохранять существенный прогресс коммитом и push каждые **45–50 минут** до дедлайна 17:50. Это требование действует для последующих контрольных точек; документация не утверждает, что такой интервал соблюдался с начала работы. Финальный push и отдельная форма сдачи обязательны независимо от расписания.

## Точная следующая контрольная точка

1. Уточнить и воспроизвести сообщённую проблему голоса на опубликованном `a3313b8`; приоритет — живой мобильный разговор и доступное управление микрофоном/клавиатурой.
2. Исправить подтверждённую причину при необходимости; проверить актуальный код, собрать, отправить в исходный репозиторий и отдельно опубликовать изменения на Vercel / Neon.
3. Проверить сохранение данных, супервизию и минимум три живые реплики в автоматическом цикле именно итогового деплоя. Сверить шестишаговый маршрут README, записать SHA и конкретные результаты, затем до 17:50 сохранить отдельную форму сдачи на платформе.
4. Оставаться на мероприятии до 18:00 согласно сообщению организатора, даже если работа сдана раньше.

Один HTTP 200, успешная сборка или высокий показатель dev-набора не подтверждают весь продукт. Отметки о production и голосе закрываются только соответствующими проверками.
