# Контрольные точки

Время ниже — 23 сентября 2026, Asia/Qyzylorda (UTC+5). Записываем только выполненные действия; список не заменяет финальную приёмку.

**Текущий production:** `bfaf300141ea47b54d602c7a3662626c63365fa5`, Vercel **Ready в 17:43, 23 сентября 2026 (Asia/Qyzylorda)**, deployment `dpl_3g2EJmw1QYZt87oLesdLYFYDHF6U`. [Приложение](https://dir-echoes-voice-router.vercel.app) · [неизменяемый адрес](https://dir-echoes-voice-router-hdu3iv35s-shadowocc.vercel.app). Финальная публикация уточняет мобильные отступы и контраст входа, документацию и материалы презентации; техническое описание и прежние проверки `901e228` ниже сохраняют свою историческую привязку. Нового benchmark или полной приёмки этим не заявляется.

**Последующее подтверждение пользователя:** связь работает («всё работает»). Это функциональное подтверждение на устройствах пользователя; отдельный протокол по браузерам/NAT, произношению RU/KK/mixed/TR и полная приёмка 40 сценариев не оформлены. Прежние отметки об ожидании ниже описывают состояние на время соответствующей проверки; измеренный SLA не заявляется.

## GitHub

| Время коммита | Commit | Содержание |
| --- | --- | --- |
| 13:33 | `00b1979` | Архитектура и план из трёх частей |
| 14:13 | `ac18111` | Приложение, постоянная история, исполнитель и роли |
| 14:39 | `cced122` | Быстрые продолжения, потоковый TTS, супервизия и подтверждения |
| 14:50 | `cbcc367` | Доказательства первой публикации и задания команде |
| 14:55 | `997edea` | Закреплённые роли, файлы, ветки и сроки сокомандников |
| 15:12 | `0661777` | Идемпотентные команды оператора, защита текстовых данных, RU/KK параметры и запуск одной командой |
| 15:28 | `cb0c595` | Смешанные RU/KK вопросы, параметры КАСКО и срока ОГПО, согласие на передачу оператору и актуализация контекста |
| 15:41 | `b7e1123` | Автоматические реплики по паузе, голосовой экран, реакция частицы и скрываемая трассировка |

Для `cced1224547148194b55c72144f234ac443592ff` после push подтверждено совпадение локального HEAD и `refs/heads/main` на origin. Это не доказательство времени начала работы: до первой записи кода выполнялись изучение материалов и настройка. Интервал 45–50 минут установлен участником для следующих контрольных точек; раннее соблюдение интервала не заявляется.

## Опубликованная версия `cced122`

- Адрес: https://dir-echoes-voice-router.vercel.app
- Vercel deployment: `dpl_7DzmVjoPHXBPGWwb4gHs6eno6De9`, статус Ready.
- Локальная и Vercel сборки завершились успешно; это не полный повторный прогон доменных тестов.
- `/` и `/api/health` вернули HTTP 200; `/api/bootstrap` без входа — 401.
- Вход супервизора, bootstrap с 40 сценариями и `/api/supervision` — HTTP 200.
- Панель качества отобразила фактическую статистику и отсутствие ручных оценок без подмены этого значения точностью модели.

### Продолжение разговора в браузере, 14:47–14:48

Сохранённый вопрос о стоимости ОГПО был открыт на опубликованном сайте. На вопрос о городе введено `almaty` — одно каноническое значение из каталога. Система сохранила параметр и спросила: «Это легковой автомобиль?».

| Метрика этого продолжения | Значение |
| --- | --- |
| Источник маршрута | `slot` / `state-fast-path` |
| Выбор маршрута | 3 мс |
| Исполнение | 14 мс |
| Серверная обработка | 107 мс |
| До первых данных TTS | 1,66 с |
| От отправки текста до первого `onplaying` | 2,72 с |
| Токены router | 0 |

Браузер не сообщил console warning/error. После перезагрузки сохранились обе реплики, параметр, trace и измерения. Событие `onplaying` подтверждает начало воспроизведения браузером; человек ещё не оценивал слышимость и произношение. Замер сделан по текстовому вводу, без STT; он не является задержкой от конца живой речи. Цель 1,5 с этим замером не достигнута. Значения одного узкого продолжения не распространяются на свободные запросы LLM.

## Остаётся перед сдачей

- Человеческая проверка живого микрофона и ответов RU/KK/mixed.
- Приёмка подтверждений, передачи оператору, ручных оценок и редактирования каталога в опубликованном интерфейсе.
- Итоговая проверка коммита сдачи, публикация его версии, сохранение SHA и URL.
- Отдельная отправка «Сдать решение» на платформе мероприятия.

## Обновление `0661777`, 15:14–15:15

- Push подтверждён: origin/main = `06617771d5d5da6e5d61f89670b5c263ceae662c`.
- Production build и TypeScript завершились успешно локально и в Vercel.
- Vercel deployment `dpl_DkW6o1hEWeL921f5kau9DPFoE9HH` получил Ready и адрес https://dir-echoes-voice-router.vercel.app.
- `/api/health`, вход супервизора, `/api/bootstrap`, `/api/supervision` вернули HTTP 200. Каталог — 40 сценариев; сохранены прежний разговор и две реплики.
- Браузер после перезагрузки показал сохранённый разговор, новый текст об использовании данных кейса и trace без повторения одинаковой причины.
- Голосовую проверку человеком пользователь передал сокоманднику; результата ещё нет. Полный повторный прогон качества router после псевдонимизации не выполнялся.

На этой контрольной точке дизайн был отложен. Позднее пользователь отдельно запросил разговор одной кнопкой, читаемый интерфейс, скрываемую трассировку и частицу с реакцией на голос. Обязательные требования и ограничения ТЗ отслеживаются в [ACCEPTANCE.md](ACCEPTANCE.md).

## Серверные исправления `cb0c595`, 15:28

- Push подтверждён: локальный HEAD и origin/main = `cb0c5956038dfda5ff25ed85b98aa9b5fdddbe28`.
- Параметры `package` и `term_months` доступны LLM и исполнителю; исходные 43 определения и хеш набора сохранены.
- Исправлены сохранение причины передачи SC34 до подтверждения, сброс после отказа и смены темы, неоднозначный запрос оператора, актуализация телефона в сохранённом контексте после подтверждённого изменения.
- Смешанные короткие уточнения и подтверждения используют RU/KK фразы без дополнительного модельного вызова.
- При push новая публикация Vercel ещё не выполнялась. Голосовой интерфейс разрабатывался отдельно и в этот коммит не включён.

## Голосовой экран и языковой контракт, 15:41–16:06

- Push `b7e11235973023b2c31c2b62acd03b125a7aaa85` подтверждён совпадением HEAD и origin/main в 15:41.
- В рабочем пакете добавлены RU/KK/TR и явные комбинации языков, детерминированные тексты подтверждения, тёмная тема и голосовая композиция с раскрываемыми подсказками.
- `pnpm typecheck` и `pnpm launch` завершились успешно: импорт сохранённого набора, проверка 40/43/31, Next production build и запуск 13301. Полный повторный прогон unit/API tests не выполнялся.
- Браузер подтвердил переключение light/dark, сохранение темы после reload, открытие прежней истории, полной переписки, подсказок и трассировки с сохранёнными метриками. Микрофон агентом не включался.
- Пользователь сообщил о проблемах при человеческой голосовой проверке; описание ожидается. Качество TR и новых смешанных пар пока не принято. Сборка не заменяет эту проверку.
- Этот раздел сам по себе не подтверждает публикацию нового пакета; SHA и deployment фиксируются отдельно после успешной загрузки.
## Обновление голосового интерфейса, 16:18

- Предыдущий этап отправлен: `4f949bcbfd90113337d4c2129315342f3644362a`, 15:59; локальный SHA совпал с origin/main.
- Следующий пакет: чистая чёрная сцена, скрываемые меню/чат/подсказки, прозрачная расшифровка поверх сцены, 3400 частиц и независимые звёзды. Во время ответа используется фактический RMS Web Audio; в ожидании предусмотрена сборка предоставленных знаков Halyk.
- В голосовом цикле добавлены калибровка фона, восстановление коротких реплик из локального буфера и переиспользование аудиоэлемента после подготовки в пользовательском жесте. Это исправления выявленных по коду рисков, а не подтверждённое устранение ещё не описанной ошибки сокомандника.
- Production build и TypeScript завершились успешно после объединения изменений. Unit/API benchmarks не запускались.
- Браузерная проверка размеров: 390×844, 844×390 и 390×500. Ширина документа совпала с viewport, поле ввода и кнопка звонка оставались в его пределах. Чат и меню показываются/скрываются; сцена при открытии чата не становится отдельной колонкой.
- Добавлены visualViewport и safe-area для экранной клавиатуры. Это не физическая проверка Safari/Android, микрофона, качества RU/KK/TR или задержки до слышимого ответа.
- README содержит самостоятельный маршрут проверки для жюри из шести шагов.

## Подтверждённая публикация `5aac0b5`

- Полный SHA: `5aac0b52419687cd58fc4c851bb30eae79a07820`.
- Vercel deployment: `dpl_3KKUhDrM3dGtMq1no3MBNuygwjtv`, статус **Ready**.
- [Production URL](https://dir-echoes-voice-router.vercel.app) и [неизменяемый URL](https://dir-echoes-voice-router-6aeb0oeyz-shadowocc.vercel.app) зафиксированы для этой публикации.
- Ведущий агент подтвердил `pnpm launch`, production-build и TypeScript, а также отдельную Vercel build. Нового полного unit/API-прогона или benchmark этим не заявляется.
- `/api/health` вернул `status: ok`. Браузер загрузил новый экран и прежнюю историю собственного разговора. Это не проверка чужого доступа и не новый сквозной вызов всех речевых сервисов.
- Проверены размеры **390×844**, **390×500**, **844×390**: горизонтального переполнения нет. Проверка CSS и браузерного viewport не равна проверке физической клавиатуры ОС, Safari или реального мобильного аудио.
- Пользователь ранее сообщил о проблемах живого голоса; описание ожидается. Публикация не считается подтверждением устранения ещё не описанного сбоя. Человеческая RU/KK/mixed-приёмка и качество TR остаются открытыми.
- Цели **500 мс** для router и **1,5 с** до голосового ответа не объявляются достигнутыми. Прежние цифры остаются привязаны к версиям и условиям измерений выше.
- На этом историческом срезе готовилась следующая правка частиц; позже она опубликована в `a3313b8`, см. запись ниже.

## Плавность и знак DIR ECHOES — пакет `a3313b8`

- Выбран пользователем знак № 4, птица из звуковой волны; исходный PNG сохранён вместе с пятью другими вариантами в `docs/brand`. Знак установлен в меню, входе и favicon, добавлен к редким формам ожидания вместе с предоставленными знаками Halyk.
- Отрисовка частиц объединена в группы, покадровая сортировка удалена. Целевая кадровая частота — 60, но измеренный FPS на устройствах не заявляется. На мобильных 1800 точек и DPR до 1,5, на больших экранах до 3400.
- Сборка и распад используют плавные переходы; дыхание остаётся в собранном знаке. Курсор влияет на основные частицы; 180 звёзд движутся отдельно и не реагируют на звук или курсор. Начало разговора даёт один разлёт; ответ собирает форму с реакцией на измеренный звук.
- Исправлена потеря короткого измеренного уровня при начале речи; окно измерения TTS увеличено. Во время калибровки микрофона частицы уже показывают его уровень, при этом надпись состояния сохраняется. Пороги распознавания и условия отправки речи не менялись.
- `pnpm build` и встроенная проверка TypeScript прошли. Это не подтверждает реальный микрофон, FPS на телефоне или достижение целевых задержек. Публикация этого пакета подтверждена записью ниже.


## Подтверждённая публикация `a3313b8`, 16:32–16:33

**Production на этой контрольной точке:** `a3313b8196d4f5a47e775077e52dbf4d68113bd9`; push **16:32:40**, локальный SHA совпадает с `origin/main`. Vercel **Ready**, deployment `dpl_AGZwHSGrQsWirGubs3m5cYWPoY7L`. [Основной адрес](https://dir-echoes-voice-router.vercel.app) · [адрес этого деплоя](https://dir-echoes-voice-router-4slrsmsra-shadowocc.vercel.app). Локальные `pnpm build` / TypeScript и Vercel build прошли; health HTTP 200, `ok`, подтверждён в **16:33**. Production после перезагрузки показал новый UI; favicon указывает на `/brand/dir-echoes-mark.png`. При фактическом viewport **600×944** горизонтального переполнения нет. Прежняя собственная история проверена локально; её повторная проверка на этом production reload не заявляется. Живой мобильный голос и системная клавиатура не приняты.

- В `a3313b8` опубликованы выбранный знак №4 «птица» в меню/входе/favicon и исправления частиц: до **3400** точек, **1800** на мобильных/ограниченных устройствах, пакетная отрисовка без покадровой сортировки. **60 FPS — цель планировщика, не измеренный FPS.** Реакция на курсор и сохранение короткого измеренного RMS исправлены в коде; 180 фоновых звёзд независимы. Это не подтверждает устранение сообщённого пользователем голосового сбоя: живая приёмка остаётся открытой.
- Историческая браузерная проверка `5aac0b5` при **390×844, 390×500 и 844×390** не выявила горизонтального переполнения. Она не переназначается новому деплою и не проверяет физическую клавиатуру ОС или живое аудио.
- Нового unit/API-прогона, измеренного FPS, проверки живого голоса или достижения 500 мс / 1,5 с не заявляется.

## Нативный звук и фон на весь экран, 16:41

- Пользователь уточнил неисправность: во встроенном браузере Codex на компьютере не слышен автоматический ответ и ручное «Слушать ответ». Прежний индикатор подключал медиаэлемент к живому AudioContext; этот риск исключён: воспроизведение теперь полностью принадлежит HTMLAudioElement, а индикатор декодирует отдельную копию завершённого MP3 через OfflineAudioContext. Измеритель не подключается к динамикам и не вызывает play/pause/load. Это исправление тракта, а не подтверждение слышимости на устройстве пользователя.
- MSE остаётся потоковым; реальный RMS доступен после получения полной копии ответа. До её готовности уровень нулевой; движение состояния не выдаётся за измеренную речь. Дополнительных запросов синтеза нет.
- Звёзды вынесены в отдельный canvas на весь viewport. Основная форма собранна при слушании, расходится при ответе и возвращается после него; амплитуда следует измеренному звуку. Переходы логотипов продлены до 4 секунд, светлая тема получила более контрастную палитру.
- `pnpm build` и TypeScript прошли после объединения этих изменений. Для замены старого аудиоэлемента после публикации нужен полный reload. Живой ответ пользователя ещё ожидается.

- Дополнительная визуальная проверка: локальный CUA viewport **1280×800**, движение/клик в пустой центральной области вызвали видимое расхождение основного облака частиц; есть снимки до/после. Это наблюдение реакции на курсор, без микрофона и без измерения FPS.


## Подтверждённая публикация `156e2f7`, 16:45–16:47

**Production на этой контрольной точке:** `156e2f70ece747097726a91471bac0dc68f9b524`; push **16:45**, локальный SHA совпадает с `origin/main`. Vercel **Ready**, deployment `dpl_DSme2S1qidZNiC6iaXSgFwhB6k9h`. [Основной адрес](https://dir-echoes-voice-router.vercel.app) · [адрес этого деплоя](https://dir-echoes-voice-router-kjb3tejqp-shadowocc.vercel.app). Локальные `pnpm build` / TypeScript прошли дважды после объединения изменений; Vercel build завершился за 9 секунд. Health HTTP 200, `ok`, подтверждён в **16:47**. Локально при viewport **390×844** звёздный canvas имеет fixed-положение (0, 0) и размер 390×844; в светлой теме основная графитовая форма видима, после проверки возвращена тёмная тема. Слышимый ответ после reload ещё ожидает человеческой проверки; физический мобильный голос и системная клавиатура не приняты.

- Нативный HTMLAudioElement и OfflineAudioContext-копия теперь опубликованы; MSE продолжается потоково. До EOF и декодирования RMS равен нулю. Дополнительный TTS ради визуализации не вызывается.
- Звёзды вынесены в отдельный fixed canvas на весь viewport (180 / 110 на мобильных); основная форма и графитовая светлая палитра вошли в этот релиз. Звёзды не реагируют на аудио и курсор, движения состояния не выдаются за RMS.
- Статические RU/KK/TR-перечисления клиентского UI заменены на «Говорите на удобном вам языке». Языковой контракт ядра остаётся RU/KK/TR; итальянский и другие языки этим не подтверждены.
- Сбой пользователя: desktop Codex IAB молчал при автоматическом и ручном TTS. Ответ человека о слышимости после reload ещё ожидается; устранение не заявляется.
- Нового unit/API-прогона, измеренного FPS, физической мобильной приёмки и достижения 500 мс / 1,5 с не заявляется. Исторические проверки геометрии и сохранённой истории выше остаются привязаны к прежним версиям.


## Подтверждённая публикация `0b9cf02`, 17:02

**Production на исторической контрольной точке:** `0b9cf024fabc8173b73573fc8c6259b449be5dab`; контрольная точка **17:02**. Vercel **Ready**, deployment `dpl_G5eEaUbVHbXixbQ2KPoVuvwVyP2M`, Vercel build — **12 секунд**. [Основной адрес](https://dir-echoes-voice-router.vercel.app) · [адрес этого деплоя](https://dir-echoes-voice-router-6zqrwinzt-shadowocc.vercel.app). Опубликованы расширение языкового контракта, профиль новой модели, исправления диалога и постоянный журнал ошибок. Новый отдельный health-замер и слышимость после reload в этой записи не подтверждаются. Исторические проверки размеров, сохранённой истории, скорости и качества ниже относятся к указанным прежним версиям.

- Расширены нормализованные ISO-коды; перевод справки отделён от проверенного RU/KK/TR согласия на операции. Явная просьба оператору допустима и на другом языке; качество новых языков не измерено.
- Включён профиль `gpt-5.4-mini`, `reasoning_effort: none`, оценка $0.75 / $0.075 / $4.50 за млн токенов. Доступность модели в аккаунте подтверждалась чтением списка; новый benchmark не проводился.
- Приветствия отделены от ошибок уточнения. Постоянный журнал ошибок сохраняет безопасные категории и ссылки независимо от retry; добавлен экран супервизии.
- Живой WebRTC клиент ↔ оператор и новый обзор разговоров разрабатываются после этого коммита; они не относятся к подтверждённому деплою. Приёмка звука, NAT и двух сторон ещё требуется.


## Публикация `9688e01`, 17:20, и последующая приёмка

**Опубликовано в `9688e01`:** TTS `marin` / `natural-v3`, локальный VAD 650 мс, источники `social` / `catalog_example`, Supervisor Live, перехват AI и браузерный WebRTC клиент ↔ оператор. Сложные деловые запросы сохраняют LLM; exact fast path не означает общую точность или скорость всего потока. Без настроенного TURN соединение через некоторые NAT не гарантируется.

**Имеющееся подтверждение:** локальный браузер при 1440×960 показал новый Live-экран, реальную сохранённую переписку в режиме просмотра и клиентскую голосовую сцену с чатом. В **17:26** пользователь подтвердил, что супервизор видит разговоры. Звонок не был найден, поскольку строка разговора ещё не выбрана. Физическая слышимость AI и WebRTC между двумя устройствами **не приняты**.

**Опубликованный пакет `901e228`:** присутствие Live по heartbeat; тема обращения из фактического контекста; единая кнопка «Принять и позвонить» с окном «Подключиться к клиенту?» и отдельным согласием клиента; новый экран входа AuthGate с двумя ролями, нейтральной slate-палитрой, зелёным акцентом и белым знаком. Исправлено согласие после приветствия: при прежнем проверенном языке повторяется сохранённый preview и явный вопрос, при смене/непроверенном языке прежнее подтверждение сбрасывается с сохранением запроса. Прямой запрос оператора отделён от ответа на старый вопрос. Сборка и Ready подтверждены; новый полный benchmark и достижение 500 мс / 1,5 с не заявляются.



## Отдельные пользовательские trace, 17:23

| Реплика | STT | Router | Доп. формулировка | TTS до первого байта | Конец речи → воспроизведение |
| --- | ---: | ---: | ---: | ---: | ---: |
| Приветствие, `social`, аудио из кэша | 598 мс | 1,738 мс | — | кэш | 1730 мс |
| Деловой запрос, LLM: наблюдение 1 | 641 мс | 1670 мс | 0 мс | 1058 мс | 4599 мс |
| Деловой запрос, LLM: наблюдение 2 | 867 мс | 1292 мс | 882 мс | 662 мс | 5012 мс |

Серверный trace приветствия — **66 мс**. Это три отдельных наблюдения реального приложения, не средние показатели и не новый benchmark. Событие воспроизведения не заменяет подтверждение физической слышимости человеком. Цели **500 мс для полного LLM-router** и **1,5 с до голоса** не достигнуты этими примерами. Исторические 104 dev-реплики не оценивают текущую модель, живую речь или новые языки.

**Публикация этой контрольной точки:** `9688e0193a5ed1ef6eabe31a191352ed1d3fce46`, Ready `dpl_2qcRbptAp9MmrbKo9upEdSQTMkwy`, 17:20. Локальные TypeScript/build и Vercel build прошли; это не новый unit/API-прогон. [Неизменяемый URL](https://dir-echoes-voice-router-mxwfhils2-shadowocc.vercel.app). Ссылка формы сдачи ожидается от участника, сохранение платформой ещё не подтверждено. Личный дедлайн 17:50; присутствие до 18:00.


## Финальная публикация `901e228`, 17:38

- Код: `901e228a6ae6f68803846336a81538e6335da5d2`, push в `origin/main` подтверждён.
- Локальные TypeScript и production-build завершились в **17:37–17:38**. Vercel присвоил **Ready в 17:38**, deployment `dpl_7ArMp5utrhteSf6VFL6x8eEZCSHR`.
- [Неизменяемый URL](https://dir-echoes-voice-router-kmhp7nji5-shadowocc.vercel.app); [основной адрес](https://dir-echoes-voice-router.vercel.app) привязан к этому deployment.
- Включены presence Live с истечением heartbeat, тема обращения, единая кнопка «Принять и позвонить», нативное окно подтверждения и отдельное принятие клиентом, новый AuthGate, исправление согласия после приветствия.
- Пользователь подтвердил работоспособность связи. Это не новый benchmark, полная приёмка RU/KK/mixed/TR, всех 40 сценариев или всех NAT. Исторические trace 17:23 и показатели 104 dev-реплик не переназначаются этой версии.
- Форму организатора ещё предстоит открыть по ссылке участника и сохранить до 17:50; её отправка этой публикацией не подтверждается. Присутствие на мероприятии — до 18:00.

Документальный коммит может следовать за опубликованным SHA кода и не изменяет привязку deployment.


## Финальная публикация документации и дизайна `bfaf300`, 17:43

- SHA: `bfaf300141ea47b54d602c7a3662626c63365fa5`.
- Vercel **Ready в 17:43**, deployment `dpl_3g2EJmw1QYZt87oLesdLYFYDHF6U`.
- [Неизменяемый URL](https://dir-echoes-voice-router-hdu3iv35s-shadowocc.vercel.app) · [основной адрес](https://dir-echoes-voice-router.vercel.app).
- Относительно `901e228` уточнены мобильные отступы и контраст экрана входа; включены документация и материалы презентации. Архитектура, голосовой и операторский механизмы описаны контрольной точкой `901e228`.
- Эта запись не является новым benchmark или повторной полной голосовой/языковой приёмкой. Исторические измерения остаются привязаны к соответствующим версиям.
- Публикация приложения не подтверждает отдельное сохранение формы организатора; статус сдачи фиксируется после подтверждения платформы.
